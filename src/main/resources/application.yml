server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_trade?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: jjs060903
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:com/campus/trade/mapper/*.xml
  type-aliases-package: com.campus.trade.pojo

jwt:
  secret: campus-trade-secret-key-2024-campus-trade-secret-key
  expire: 86400000